<UserControl x:Class="WPF.MessageShow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    
    <DockPanel>
        <Grid Background="{StaticResource Panel_2}" DockPanel.Dock="Top" Height="22">
            <TextBlock Text="Message" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource White2}" FontWeight="Black" FontSize="14"/>
        </Grid>
        <RichTextBox x:Name="rtb_msage" Margin="0,1,0,0" IsReadOnly="True" VerticalScrollBarVisibility="Visible" TextChanged="RichTextBox_TextChanged"
            Background="{StaticResource Panel_2}"  Foreground="{DynamicResource Gray}" BorderBrush="#00000000" BorderThickness="0">
            <FlowDocument>
                <Paragraph x:Name="txt_message_Paragraph" />
            </FlowDocument>
        </RichTextBox>
    </DockPanel>
</UserControl>
